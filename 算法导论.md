### 二、欧拉路
#### 1. 无向图最小字典序欧拉路判定
```c++
const int N = 501;
int n;
multiset<int,greater<int>> e[N];
stack<int> s;
int deg[N];
int in = 501;
int les = 501;
int mx;

void dfs(int u) {
    while (!e[u].empty()) {
        int v = *(e[u].rbegin());
        e[u].erase(e[u].find(v));
        e[v].erase(e[v].find(u));
        dfs(v);
    }
    s.push(u);
}

void solve() {
    cin >> n;
    for (int i = 0;i < n;++i) {
        int u, v;
        cin >> u >> v;
        e[u].insert(v);
        e[v].insert(u);
        deg[u]++;
        deg[v]++;
        mx = max(mx, u);
        mx = max(mx, v);
        les = min(les, u);
        les = min(les, v);
    }
    
    for (int i = 1;i <= n;++i)
        if (deg[i] % 2 != 0)
            in = min(in, i);
    
    if (in == 501) dfs(les);
    else           dfs(in);

    while (!s.empty()) {
        cout << s.top() << endl;
        s.pop();
    }
}
```
### 七、最小环
#### 1. 无向图 最小环 floyd
```c++
const ll N = 101;
ll n, m;
ll w[N][N];
ll d[N][N];
ll ans = INF;

inline void solve() {
    cin >> n >> m;
    for (ll i = 1;i <= n;++i)
        for (ll j = 1;j <= n;++j) {
            if (i == j) continue;
            w[i][j] = INF;
        }
    for (ll i = 1;i <= m;++i) {
        ll u, v, dis;
        cin >> u >> v >> dis;
        w[u][v] = w[v][u] = dis;
    }
    memcpy(d, w, sizeof(w));
    for (ll k = 1;k <= n;++k) {
        for (ll i = 1;i < k;++i)
            for (ll j = i + 1;j < k;++j)
                ans = min(ans, d[i][j] + w[j][k] + w[k][i]);
        for (ll i = 1;i <= n;++i)
            for (ll j = 1;j <= n;++j)
                d[i][j] = min(d[i][j], d[i][k] + d[k][j]);
    }
    if (ans == INF) cout << "No solution." << endl;
    else cout << ans << endl;
}
```
## 扫描线
### 一、扫描线求面积并
#### 1. 示例1
```c++
const int N = 1e5 + 10;

typedef struct {
    int l, r;
    int cnt;
    int val;
}segtree;
segtree tr[N << 4];

typedef struct {
    int x1, x2;
    int y;
    int op;
}Node;
Node p[N << 1];
ll a[N << 1];
int n;

bool cmp(Node& x, Node& y) {
    return x.y < y.y;
}

void build(int p, int l, int r) {
    tr[p] = { l,r,0,0 };
    if (l == r) return;
    int m = l + r >> 1;
    build(lc, l, m);
    build(rc, m + 1, r);
}

void pushup(int p) {
    int l = tr[p].l;
    int r = tr[p].r;
    if (tr[p].cnt) tr[p].val = a[r + 1] - a[l];
    else tr[p].val = tr[lc].val + tr[rc].val;
}

void modify(int p, int l, int r, int op) {
    if (l <= tr[p].l && tr[p].r <= r) {
        tr[p].cnt += op;
        pushup(p);
        return;
    }
    int m = tr[p].l + tr[p].r >> 1;
    if (l <= m) modify(lc, l, r, op);
    if (r > m) modify(rc, l, r, op);
    pushup(p);
}

void solve() {
    cin >> n;
    for (int i = 1;i <= n;++i) {
        int x1, y1, x2, y2;
        cin >> x1 >> y1 >> x2 >> y2;
        p[i] = { x1,x2,y1,1 };
        p[i + n] = { x1,x2,y2,-1 };
        a[i] = x1, a[i + n] = x2;
    }
    n <<= 1;
    sort(p + 1, p + n + 1, cmp);
    sort(a + 1, a + n + 1);
    //去重离散化
    int s = unique(a + 1, a + n + 1) - a - 1;
    build(1, 1, s - 1);
    ll ans = 0;
    for (int i = 1;i < n;++i) {
        int l = lower_bound(a + 1, a + s + 1, p[i].x1) - a;
        int r = lower_bound(a + 1, a + s + 1, p[i].x2) - a;
        modify(1, l, r - 1, p[i].op);
        ans += 1ll * tr[1].val * (p[i + 1].y - p[i].y);
    }

    cout << ans << endl;
}
```
### 二、扫描线求周长并
#### 1. 示例1
```c++
const int N = 1e5 + 10;

typedef struct {
    int l, r;
    int cnt, len;
    int sum;
    bool lcover, rcover;
}segtree;
segtree tr[N << 4];

typedef struct {
    int x1, x2;
    int y;
    int op;
}Node;
Node p[N << 1];
ll a[N << 1];
int n;

bool cmp(Node& x, Node& y) {
    return x.y < y.y;
}

void build(int p, int l, int r) {
    tr[p] = { l,r,0,0 };
    if (l == r) return;
    int m = l + r >> 1;
    build(lc, l, m);
    build(rc, m + 1, r);
}

void pushup(int p) {
    int l = tr[p].l;
    int r = tr[p].r;
    if (tr[p].cnt) {
        tr[p].len = a[r + 1] - a[l];
        tr[p].sum = 2;
        tr[p].lcover = tr[p].rcover = true;
    }
    else {
        tr[p].len = tr[lc].len + tr[rc].len;
        tr[p].sum = tr[lc].sum + tr[rc].sum;
        tr[p].lcover = tr[lc].lcover;
        tr[p].rcover = tr[rc].rcover;
        if (tr[lc].rcover && tr[rc].lcover)
            tr[p].sum -= 2;
    }
}

void modify(int p, int l, int r, int op) {
    if (l <= tr[p].l && tr[p].r <= r) {
        tr[p].cnt += op;
        pushup(p);
        return;
    }
    int m = tr[p].l + tr[p].r >> 1;
    if (l <= m) modify(lc, l, r, op);
    if (r > m) modify(rc, l, r, op);
    pushup(p);
}

void solve() {
    cin >> n;
    for (int i = 1;i <= n;++i) {
        int x1, y1, x2, y2;
        cin >> x1 >> y1 >> x2 >> y2;
        p[i] = { x1,x2,y1,1 };
        p[i + n] = { x1,x2,y2,-1 };
        a[i] = x1, a[i + n] = x2;
    }
    n <<= 1;
    sort(p + 1, p + n + 1, cmp);
    sort(a + 1, a + n + 1);
    //去重离散化
    int s = unique(a + 1, a + n + 1) - a - 1;
    build(1, 1, s - 1);
    ll ans = 0;
    for (int i = 1;i < n;++i) {
        int l = lower_bound(a + 1, a + s + 1, p[i].x1) - a;
        int r = lower_bound(a + 1, a + s + 1, p[i].x2) - a;
        ll past = tr[1].len;
        modify(1, l, r - 1, p[i].op);
        ans += llabs(tr[1].len - past);
        ans += tr[1].sum * (p[i + 1].y - p[i].y);
    }
    ans += p[n].x2 - p[n].x1;//最后一条扫描线
    cout << ans << endl;
}
```
## 树上问题

#### 3. 树形dp求树的直径 （链式前向星版本）
```c++
const int N = 2e5 + 1;
int n;
typedef struct {
    int to;
    int w;
    int next;
}Edge;
Edge e[N<<1];
int cnt;
int h[N];
int ans;

void add(int u, int v, int w) {
    e[++cnt].to = v;
    e[cnt].w = w;
    e[cnt].next = h[u];
    h[u] = cnt;
}

int dfs(int u, int fa) {
    int d1, d2;
    d1 = d2 = 0;
    for (int i = h[u];i;i = e[i].next) {
        int v = e[i].to;
        if (v == fa) continue;
        int d = dfs(v, u) + e[i].w;
        if (d >= d1) d2 = d1, d1 = d;
        else if (d > d2) d2 = d;
    }
    ans = max(ans, d1 + d2);
    return d1;
}

void solve() {
    cin >> n;
    for (int i = 1;i < n;++i) {
        int u, v, w;
        cin >> u >> v >> w;
        add(u, v, w);
        add(v, u, w);
    }

    dfs(1, 0);
    cout << ans << endl;
}
```
### 三、树的重心
```c++
const int N = 1e5 + 1;
int n;
vector<int> edge[N];
int ans = INTMAX;//答案

//dfs返回以u为根节点的子树的节点个数
int dfs(int u, int fa) {
    int t = 1;
    int mx = 0;
    for (auto v : edge[u]) {
        if (v == fa) continue;
        int sub = dfs(v, u);
        t += sub;
        mx = max(mx, sub);//记录最大的子树节点个数
    }
    mx = max(mx, n - t);//记录最大子树个数，包括上方子树
    ans = min(ans, mx);
    return t;
}

void solve() {
    cin >> n;
    for (int i = 1;i <= n;++i) {
        int u, v;
        cin >> u >> v;
        edge[u].push_back(v);
        edge[v].push_back(u);
    }

    dfs(1, 0);

    cout << ans << endl;
}
```
### 五、树上启发式合并
#### 1. 例：颜色平衡树
```c++
const int N = 5e5 + 1;
int n;
vector<int> e[N];
int sz[N],son[N];
int color[N];
int t[N];
int cnt[N];
int ans;

//移除轻子树贡献
void rem(int u) {
    t[cnt[color[u]]]--;
    cnt[color[u]]--;
    t[cnt[color[u]]]++;
    for (auto v : e[u])
        rem(v);
}

//加上轻子树的贡献
void add(int u) {
    t[cnt[color[u]]]--;
    cnt[color[u]]++;
    t[cnt[color[u]]]++;
    for (auto v : e[u])
        add(v);
}

//处理出重儿子
void dfs(int u) {
    sz[u] = 1;
    for (auto v : e[u]) {
        dfs(v);
        sz[u] += sz[v];
        if (sz[son[u]] < sz[v]) son[u] = v;
    }
}

void dfs2(int u, int erase) {
    //先遍历轻儿子，不保留对重子树的贡献
    //轻子树上的答案是在第一次遍历时得到的
    for (auto v : e[u]) {
        if (v == son[u]) continue;
        dfs2(v,1);
    }
    //再遍历重子树，保留贡献
    if (son[u]) dfs2(son[u], 0);

    t[cnt[color[u]]]--;
    cnt[color[u]]++;
    t[cnt[color[u]]]++;

    //加上轻子树的贡献
    for (auto v : e[u]) {
        if (v == son[u]) continue;
        add(v);
    }
    //判断是否为颜色平衡树
    if (cnt[color[u]] * t[cnt[color[u]]] == sz[u]) ans++;
    //移除以该节点为根地子树的贡献
    if (erase) rem(u);
}

void solve() {
    cin >> n;
    for (int i = 1;i <= n;++i) {
        int fa;
        cin >> color[i] >> fa;
        e[fa].emplace_back(i);//单向树
    }

    dfs(1);
    dfs2(1,0);

    cout << ans << endl;
}
```
#### 2. 例：XOR Tree
```c++
const int N = 2e5 + 1;
vector<int> e[N];
set<int> s[N];
int n, a[N], d[N];
int ans;

void dfs(int u, int fa) {
    d[u] = d[fa] ^ a[u];
    s[u].insert(d[u]);
    bool flag = 0;
    for (auto v : e[u]) {
        if (v == fa) continue;
        dfs(v, u);
        if (s[u].size() < s[v].size())
            swap(s[u], s[v]);
        for (auto elem : s[v])
            if (s[u].find(elem ^ a[u]) != s[u].end()) {
                flag = 1;
                break;
            }
        s[u].insert(s[v].begin(), s[v].end());
    }
    if (flag) ans++, s[u].clear();
}

void solve() {
    cin >> n;
    for (int i = 1;i <= n;++i)
        cin >> a[i];
    for (int i = 1;i < n;++i) {
        int u, v;
        cin >> u >> v;
        e[u].push_back(v);
        e[v].push_back(u);
    }
    dfs(1, 0);
    cout << ans << endl;
}
```
## 数据结构
### 一、前缀和
#### 1. 树上点前缀和
```c++
const ll MOD = 998244353;
const ll N = 3e5 + 1;
ll n, m;
vector<ll> e[N];
ll dep[N];
ll ps[N][51];
ll sz[N];
ll father[N];
ll son[N];
ll top[N];

ll qpow(ll base, ll p) {
    ll ans = 1;
    while (p) {
        if (p & 1) ans = ans * base % MOD;
        base = base * base % MOD;
        p >>= 1;
    }
    return ans;
}

void dfs(ll u, ll fa) {
    father[u] = fa;
    sz[u] = 1;
    dep[u] = dep[fa] + 1;
    for (ll i = 0;i <= 50;++i)
        ps[u][i] = (ps[u][i] + qpow(dep[u], i)) % MOD;
    for (auto v : e[u]) {
        if (v == fa) continue;
        for (ll i = 1;i <= 50;++i) ps[v][i] += ps[u][i];
        dfs(v, u);
        sz[u] += sz[v];
        if (sz[son[u]] < sz[v]) son[u] = v;
    }
}

void dfs2(ll u, ll t) {
    top[u] = t;
    if (son[u]) dfs2(son[u], t);
    for (auto v : e[u]) {
        if (v == father[u] || v == son[u]) continue;
        dfs2(v, v);
    }
}

ll lca(ll u, ll v) {
    while (top[u] != top[v]) {
        if (dep[top[u]] < dep[top[v]]) swap(u, v);
        u = father[top[u]];
    }
    return dep[u] < dep[v] ? u : v;
}

void solve() {
    cin >> n;
    for (ll i = 1;i < n;++i) {
        ll u, v;
        cin >> u >> v;
        e[u].emplace_back(v);
        e[v].emplace_back(u);
    }
    dep[0] = -1;
    dfs(1, 0);
    dfs2(1, 1);
    cin >> m;
    for (ll i = 1;i <= m;++i) {
        ll u, v, k;
        cin >> u >> v >> k;
        ll ans = ((ps[u][k] + ps[v][k] - ps[lca(u, v)][k] - ps[father[lca(u, v)]][k]) % MOD + MOD) % MOD;
        cout << ans << endl;
    }
}
```
#### 2. 一维前缀和
```c++
const int N = 1e5 + 1;
ll a[N], ps[N];
int n, m;

void solve() {
    cin >> n;
    for (int i = 1;i <= n;++i)
        cin >> a[i];
    for (int i = 1;i <= n;++i) {
        ps[i] = ps[i - 1] + a[i];
    }
    cin >> m;

    for (int i = 1;i <= m;++i) {
        int l, r;
        cin >> l >> r;
        cout << ps[r] - ps[l - 1] << endl;
    }
}
```
### 二、树状数组
#### 1. 点修改 区间查询
```c++
const ll N = 5e5 + 1;
typedef struct {
    ll value;
    ll rank;
}Node;
Node a[N];
ll n;
ll ans;
ll TreeArray[N];

bool cmp(Node x, Node y) {
    if (x.value == y.value)
        return x.rank > y.rank;
    return x.value > y.value;
}

void add(ll x) {
    for (;x <= n;x += (x & (-x)))
        TreeArray[x] += 1;
}

ll query(ll x) {
    ll sum = 0;
    for (;x;x -= (x & (-x)))
        sum += TreeArray[x];
    return sum;
}

void solve() {
    cin >> n;
    for (ll i = 1;i <= n;++i) {
        cin >> a[i].value;
        a[i].rank = i;
    }
    sort(a + 1, a + n + 1, cmp);
    for (ll i = 1;i <= n;++i) {
        add(a[i].rank);
        ans += query(a[i].rank - 1);
    }
    cout << ans << endl;
}
```
#### 2. 树状数组维护康托展开
```c++
const int MOD = 998244353;
const int N = 1e6 + 1;
int n;
ll a[N];
ll TreeArray[N];
ll o[N];

//预处理阶乘
void init() {
    o[0] = 1;
    for (int i = 1;i <= n;++i)
        o[i] = o[i - 1] * i % MOD;
}

//树状数组更新
void update(int locate, int add_val) {
    for (int i = locate;i <= n;i += (i & (-i)))
        TreeArray[i] += add_val;
}
//树状数组查询前缀和
int query(int locate) {
    ll ans = 0;
    for (int i = locate;i;i -= (i & (-i)))
        ans += TreeArray[i];
    return ans;
}

void solve() {
    cin >> n;
    init();
    for (int i = 1;i <= n;++i) {
        cin >> a[i];
        update(i, 1);
    }
    ll ans = 0;
    for (int i = 1;i <= n;++i) {
        ans = (ans + (query(a[i] - 1)) % MOD * o[n - i] % MOD) % MOD;
        update(a[i], -1);
    }
    cout << (ans + MOD + 1) % MOD << endl;
}
```
#### 3. 区间颜色种数统计
```c++
//统计指定区间内出现颜色的种数
const int N = 1e6 + 1;
int n, m;
int c[N];
int vis[N];

typedef struct {
    int l, r;
    int idx;
}Query;
Query q[N];
int ans[N];
bool cmp(Query& a, Query& b) {
    return a.r < b.r;
}

int tr[N];
void update(int idx, int val) {
    for (;idx < N;idx += (idx & -idx))
        tr[idx] += val;
}
int query(int idx) {
    int ans = 0;
    for (;idx;idx -= (idx & -idx))
        ans += tr[idx];
    return ans;
}

void solve() {
    cin >> n;
    for (int i = 1;i <= n;++i) cin >> c[i];
    cin >> m;
    for (int i = 1;i <= m;++i) {
        cin >> q[i].l >> q[i].r;
        q[i].idx = i;
    }
    sort(q + 1, q + m + 1, cmp);
    for (int r = 1, i = 1;r <= n;++r) {
        if (vis[c[r]])
            update(vis[c[r]], -1);
        update(r, 1);
        vis[c[r]] = r;
        for (;q[i].r == r;++i)
            ans[q[i].idx] = query(q[i].r) - query(q[i].l - 1);
    }
    for (int i = 1;i <= m;++i)
        cout << ans[i] << endl;
}
```
### 四、单调队列
#### 2. 二维单调队列
```c++
const int N = 1001;
int a, b, n;
int arr[N][N];
int qmx[N], hmx, tmx;
int qmn[N], hmn, tmn;
int mx[N][N];
int mn[N][N];

int ans = INT_MAX;

void solve() {
    cin >> a >> b >> n;
    for (int i = 1;i <= a;++i)
        for (int j = 1;j <= b;++j)
            cin >> arr[i][j];

    for (int i = 1;i <= a;++i) {
        hmx = hmn = 1;
        tmx = tmn = 0;
        for (int j = 1;j <= b;++j) {
            while (hmx <= tmx && qmx[hmx] <= j - n) hmx++;
            while (hmx <= tmx && arr[i][qmx[tmx]] <= arr[i][j]) tmx--;
            qmx[++tmx] = j;

            while (hmn <= tmn && qmn[hmn] <= j - n) hmn++;
            while (hmn <= tmn && arr[i][qmn[tmn]] >= arr[i][j]) tmn--;
            qmn[++tmn] = j;

            if (j >= n) {
                mx[i][j - n + 1] = arr[i][qmx[hmx]];
                mn[i][j - n + 1] = arr[i][qmn[hmn]];
            }
        }
    }

    for (int j = 1;j <= b - n + 1;++j) {
        hmx = hmn = 1;
        tmx = tmn = 0;
        for (int i = 1;i <= a;++i) {
            while (hmx <= tmx && qmx[hmx] <= i - n) hmx++;
            while (hmx <= tmx && mx[qmx[tmx]][j] <= mx[i][j]) tmx--;
            qmx[++tmx] = i;

            while (hmn <= tmn && qmn[hmn] <= i - n) hmn++;
            while (hmn <= tmn && mn[qmn[tmn]][j] >= mn[i][j]) tmn--;
            qmn[++tmn] = i;

            if (i >= n)
                ans = min(ans, mx[qmx[hmx]][j] - mn[qmn[hmn]][j]);
        }
    }
    cout << ans << endl;
}
```
### 五、st表
```c++
const int N = 1e5 + 1;
int n, m;
int a[N];
int st[N][18];

void solve() {
    cin >> n >> m;
    for (int i = 1;i <= n;++i)
        cin >> st[i][0];

    for (int t = 1;t <= 17;++t)
        for (int i = 1;i + (1 << t) - 1 <= n;++i)
            st[i][t] = max(st[i][t - 1], st[i + (1 << (t - 1))][t - 1]);

    for (int i = 1;i <= m;++i) {
        int l, r;
        cin >> l >> r;
        int t = log2((r - l + 1));
        cout << max(st[l][t], st[r - (1 << t) + 1][t]) << endl;
    }
}
```
### 六、线段树
#### 4. 树链剖分 + 线段树
```c++
#define lc (p<<1)
#define rc (p<<1|1)
const int N = 1e5 + 1;

typedef struct {
    ll l, r;
    ll add;
    ll sum;
}segtree;
segtree tr[N << 2];

ll n, m, r, p;
ll a[N];
vector<ll> e[N];
ll fa[N], son[N], dep[N], sz[N];
ll cnt;
ll top[N], id[N], nw[N];

//预处理
void dfs1(ll u, ll father) {
    fa[u] = father;
    sz[u] = 1;
    dep[u] = dep[father] + 1;

    for (auto v : e[u]) {
        if (v == father) continue;
        dfs1(v, u);
        sz[u] += sz[v];
        if (sz[son[u]] < sz[v]) son[u] = v;
    }
}

//树链剖分重映射
void dfs2(ll u, ll t) {
    id[u] = ++cnt;
    nw[cnt] = a[u];
    top[u] = t;
    if (!son[u]) return;
    dfs2(son[u], t);
    for (auto v : e[u]) {
        if (v == fa[u] || v == son[u]) continue;
        dfs2(v, v);
    }
}

//向上更新
void pushup(ll p) {
    tr[p].sum = tr[lc].sum + tr[rc].sum;
}

//向下更新
void pushdown(ll p) {
    if (tr[p].add) {
        tr[lc].sum = (tr[lc].sum + (tr[lc].r - tr[lc].l + 1) * tr[p].add);
        tr[rc].sum = (tr[rc].sum + (tr[rc].r - tr[rc].l + 1) * tr[p].add);
        tr[lc].add += tr[p].add;
        tr[rc].add += tr[p].add;
        tr[p].add = 0;
    }
}

//线段树建树
void build(ll p, ll l, ll r) {
    tr[p] = { l,r,0,nw[l] };
    if (l == r) return;
    ll m = l + r >> 1;
    build(lc, l, m);
    build(rc, m + 1, r);
    pushup(p);
}

//线段树修改
void update(ll p, ll l, ll r, ll add_val) {
    if (tr[p].l >= l && tr[p].r <= r) {
        tr[p].sum = (tr[p].sum + (tr[p].r - tr[p].l + 1) * add_val);
        tr[p].add += add_val;
        return;
    }
    ll m = tr[p].l + tr[p].r >> 1;
    pushdown(p);
    if (l <= m) update(lc, l, r, add_val);
    if (r > m) update(rc, l, r, add_val);
    pushup(p);
}

//树上路径修改
void update_path(ll u, ll v, ll add_val) {
    while (top[u] != top[v]) {
        if (dep[top[u]] < dep[top[v]]) swap(u, v);
        update(1, id[top[u]], id[u], add_val);
        u = fa[top[u]];
    }
    if (dep[u] < dep[v]) swap(u, v);
    update(1, id[v], id[u], add_val);
}

//子树修改
void update_tr(ll u, ll add_val) {
    update(1, id[u], id[u] + sz[u] - 1, add_val);
}

//线段树查询
ll query(ll p, ll l, ll r) {
    if (tr[p].l >= l && tr[p].r <= r)
        return tr[p].sum;
    ll m = tr[p].l + tr[p].r >> 1;
    pushdown(p);
    ll ans = 0;
    if (l <= m) ans += query(lc, l, r);
    if (r > m) ans += query(rc, l, r);
    return ans;
}

//树上路径查询查询
int query_path(ll u, ll v) {
    ll ans = 0;
    while (top[u] != top[v]) {
        if (dep[top[u]] < dep[top[v]]) swap(u, v);
        ans += query(1, id[top[u]], id[u]);
        ans %= p;
        u = fa[top[u]];
    }
    if (dep[u] < dep[v]) swap(u, v);
    ans += query(1, id[v], id[u]);
    return ans %p;
}

//子树查询
ll query_tr(ll u) {
    return query(1, id[u], id[u] + sz[u] - 1)%p;
}

void solve() {
    cin >> n >> m >> r >> p;
    for (int i = 1;i <= n;++i)
        cin >> a[i];
    for (int i = 1;i < n;++i) {
        int u, v;
        cin >> u >> v;
        e[u].push_back(v);
        e[v].push_back(u);
    }

    dfs1(r, 0);
    dfs2(r, r);
    build(1, 1, cnt);

    for (int i = 0;i < m;++i) {
        int t;cin >> t;
        if (t == 1) {
            int x, y, z;
            cin >> x >> y >> z;
            update_path(x, y, z);
        }
        else if (t == 2) {
            int x, y;
            cin >> x >> y;
            cout << query_path(x, y) % p << endl;
        }
        else if (t == 3) {
            int x, z;
            cin >> x >> z;
            update_tr(x, z);
        }
        else {
            int x;cin >> x;
            cout << query_tr(x) % p << endl;
        }
    }
}
```
#### 5. 线段树 维护区间最大子段和
```c++
#define lc (p<<1)
#define rc (p<<1|1)

const int N = 5e4 + 1;
int n, m;
int a[N];

typedef struct {
    int l, r;
    int mx, lmx, rmx;
    int total;//段值总和
}segtree;
segtree tr[N << 2];

void pushup(segtree& p, segtree l, segtree r) {
    p.total = l.total + r.total;
    p.lmx = max(l.lmx, l.total + r.lmx);
    p.rmx = max(r.rmx, l.rmx + r.total);
    p.mx = max(l.rmx + r.lmx, max(l.mx, r.mx));
}

void build(int p, int l, int r) {
    tr[p].l = l;
    tr[p].r = r;
    if (l == r) {
        tr[p].total = tr[p].lmx = tr[p].rmx = tr[p].mx = a[l];
        return;
    }
    else
        tr[p].total = tr[p].lmx = tr[p].rmx = tr[p].mx = -INF;
    int m = l + r >> 1;
    build(lc, l, m);
    build(rc, m + 1, r);
    pushup(tr[p], tr[lc], tr[rc]);
}

segtree query(int p, int l, int r) {
    if (l > tr[p].r || r < tr[p].l)
        return { tr[p].l,tr[p].r,-INF,-INF,-INF,0 };
    if (l <= tr[p].l && tr[p].r <= r)
        return tr[p];
    segtree T;
    pushup(T, query(lc, l, r), query(rc, l, r));
    return T;
}

void update(int p, int idx, int val) {
    if (tr[p].l == tr[p].r) {
        tr[p].total = tr[p].lmx = tr[p].rmx = tr[p].mx = val;
        return;
    }
    int m = tr[p].l + tr[p].r >> 1;
    if (idx <= m) update(lc, idx, val);
    else update(rc, idx, val);
    pushup(tr[p], tr[lc], tr[rc]);
}

void solve() {
    cin >> n;
    for (int i = 1;i <= n;++i) cin >> a[i];
    build(1, 1, n);
    tr[0] = { 0,0,-INF,-INF,-INF,0 };
    cin >> m;
    for (int i = 1;i <= m;++i) {
        int op; cin >> op;
        int l, r;cin >> l >> r;
        if (op) cout << query(1, l, r).mx << endl;
        else update(1, l, r);
    }
}
```
#### 6. 权值线段树优化dp
```c++
#define lc p<<1
#define rc p<<1|1

const int N = 5e5 + 1;
int a[N];
int dp[N];
int n, d;

typedef struct {
    int l, r;
    int mx;
}Segtree;
Segtree tr[N << 2];

void pushup(int p) {
    tr[p].mx = max(tr[lc].mx, tr[rc].mx);
}

void build(int p, int l, int r) {
    tr[p] = { l,r,0 };
    if (l == r) return;
    int m = l + r >> 1;
    build(lc, l, m);
    build(rc, m + 1, r);
    pushup(p);
}

void update(int p, int val, int mx) {
    if (tr[p].l == tr[p].r) {
        tr[p].mx = max(mx, tr[p].mx);
        return;
    }
    int m = tr[p].l + tr[p].r >> 1;
    if (val <= m) update(lc, val, mx);
    else update(rc, val, mx);
    pushup(p);
}

int query(int p, int l, int r) {
    if (l <= tr[p].l && r >= tr[p].r)
        return tr[p].mx;
    int ans = 0;
    int m = tr[p].l + tr[p].r >> 1;
    if (l <= m) ans = max(ans, query(lc, l, r));
    if (r > m) ans = max(ans, query(rc, l, r));
    return ans;
}

void solve() {
    cin >> n >> d;
    for (int i = 1;i <= n;++i)
        cin >> a[i];
    build(1, 1, N);
    for (int i = 1;i <= n;++i) {
        int l = a[i] - d;
        int r = a[i] + d;
        l = max(0, l);
        r = min(N, r);
        int mx = query(1, l, r);
        update(1, a[i], mx + 1);
    }

    cout << tr[1].mx << endl;
}
```
### 七、可持久化数据结构
#### 1. 可持久化权值线段树（主席树）
```c++
//离散化 主席树 区间第k小 o(nlog^2(n))
#define lc(x) tr[x].lc
#define rc(x) tr[x].rc

const int N = 2e5 + 5;
int n, m, a[N];
vector<int> v;
typedef struct {
    int lc, rc;
    int cnt;
}SegTree;
SegTree tr[N * 22];
int root[N], idx;
//离散化存值
int getid(int x) {
    return lower_bound(v.begin(), v.end(), x) - v.begin() + 1;
}

//第零版本创建
void build(int& x, int l, int r) {
    x = ++idx;
    if (l == r) return;
    int m = l + r >> 1;
    build(lc(x), l, m);
    build(rc(x), m + 1, r);
}

//后续版本创建在前一版本的基础上
void insert(int x, int& y, int l, int r, int v) {
    y = ++idx;
    tr[y] = tr[x];
    tr[y].cnt++;
    if (l == r) return;
    int m = l + r >> 1;
    if (v <= m) insert(lc(x), lc(y), l, m, v);
    else insert(rc(x), rc(y), m + 1, r, v);
}

//查询时双指针同步跳，通过计算两版本间的元素个数的差值判断第k小
int query(int x, int y, int l, int r, int k) {
    if (l == r) return l;
    int m = l + r >> 1;
    int s = tr[lc(y)].cnt - tr[lc(x)].cnt;
    if (k <= s) return query(lc(x), lc(y), l, m, k);
    else return query(rc(x), rc(y), m + 1, r, k - s);
}

void solve() {
    cin >> n >> m;
    for (int i = 1;i <= n;++i) {
        cin >> a[i];
        v.push_back(a[i]);//离散化
    }
    sort(v.begin(), v.end());
    v.erase(unique(v.begin(), v.end()), v.end());

    build(root[0], 1, v.size());
    for (int i = 1;i <= n;++i)
        insert(root[i - 1], root[i], 1, v.size(), getid(a[i]));
    for (int i = 1;i <= m;++i) {
        int l, r, rk;
        cin >> l >> r >> rk;
        cout << v[query(root[l - 1], root[r], 1, v.size(), rk) - 1] << endl;
    }
}
```
#### 2. 可持久化普通线段树（可持久化数组）
```c++
const int N = 1e6 + 1;
int n, m;
int a[N];
//可持久化数组
typedef struct {
    int lc, rc;
    int val;
}segtree;
segtree tr[N * 23];
int root[N << 2];
int idx;

void build(int& x, int l, int r) {
    x = ++idx;
    tr[x].val = a[l];
    if (l == r) return;
    int m = l + r >> 1;
    build(lc(x), l, m);
    build(rc(x), m + 1, r);
}

void update(int& x, int y, int loc, int val, int l, int r) {
    x = ++idx;
    tr[x] = tr[y];
    if (l == r) {
        tr[x].val = val;
        return;
    }
    int m = l + r >> 1;
    if (loc <= m) update(lc(x), lc(y), loc, val, l, m);
    else update(rc(x), rc(y), loc, val, m + 1, r);
}

int query(int x, int loc, int l, int r) {
    if (l == r) return tr[x].val;
    int m = l + r >> 1;
    if (loc <= m) return query(lc(x), loc, l, m);
    else return query(rc(x), loc, m + 1, r);
}

void solve() {
    cin >> n >> m;
    for (int i = 1;i <= n;++i)
        cin >> a[i];
    int nowver = 0;
    build(root[0], 1, n);
    for (int i = 1;i <= m;++i) {
        int ver;cin >> ver;
        int op;cin >> op;
        if (op == 1) {
            int loc, val;
            cin >> loc >> val;
            update(root[++nowver], root[ver], loc, val, 1, n);
        }
        else {
            root[++nowver] = root[ver];
            int loc;cin >> loc;
            cout << query(root[nowver], loc, 1, n) << endl;
        }
    }
}
```
#### 3. 可持久化字典树
```c++
const int len = 23;
const int N = 6e5 + 1;
int n, m;
int s[N];
int idx;
int root[N];//版本入口
int ver[N * 25];//版本号
int ch[N * 25][2];//可持久化0-1Trie

void insert(int x, int y, int i) {
    ver[x] = i;
    for (int k = len;k >= 0;--k) {
        int c = (s[i] >> k) & 1;
        ch[x][!c] = ch[y][!c];
        ch[x][c] = ++idx;
        x = ch[x][c];
        y = ch[y][c];
        ver[x] = i;
    }
}

int query(int x, int L, int v) {
    int res = 0;
    for (int k = len;k >= 0;--k) {
        int c = (v >> k) & 1;
        if (ver[ch[x][!c]] >= L)
            x = ch[x][!c], res += 1 << k;
        else
            x = ch[x][c];
    }
    return res;
}

void solve() {
    cin >> n >> m;
    ver[0] = -1;//未分配节点的版本为-1
    root[0] = ++idx;
    insert(root[0], 0, 0);
    for (int i = 1;i <= n;++i) {
        int t;cin >> t;
        s[i] = s[i - 1] ^ t;
        root[i] = ++idx;
        insert(root[i], root[i - 1], i);
    }

    for (int i = 1;i <= m;++i) {
        string S;cin >> S;
        if (S == "A") {
            int t;cin >> t;
            root[++n] = ++idx;
            s[n] = s[n - 1] ^ t;
            insert(root[n], root[n - 1], n);
        }
        else if (S == "Q") {
            int l, r, x;
            cin >> l >> r >> x;
            cout << query(root[r - 1], l - 1, s[n] ^ x) << endl;
        }
    }
}
```
#### 4. 可持久化并查集
```c++
const int N = 1e5 + 10;
int n, m;

typedef struct {
    int lc, rc;
    int val;
}segtree;
segtree f[N * 64], rk[N * 64];
int rootf[N << 1], rootrk[N << 1];
int cntf, cntrk;

void buildf(int& x, int l, int r) {
    x = ++cntf;
    if (l == r) {
        f[x].val = l;
        return;
    }
    int m = l + r >> 1;
    buildf(lc(f, x), l, m);
    buildf(rc(f, x), m + 1, r);
}

void buildrk(int& x, int l, int r) {
    x = ++cntrk;
    if (l == r) {
        rk[x].val = 1;
        return;
    }
    int m = l + r >> 1;
    buildrk(lc(rk, x), l, m);
    buildrk(rc(rk, x), m + 1, r);
}

int find(int x, int tar, int l, int r) {
    if (l == r) return f[x].val;
    int m = l + r >> 1;
    if (tar <= m) return find(lc(f, x), tar, l, m);
    else return find(rc(f, x), tar, m + 1, r);
}

int queryf(int x, int tar) {
    int fa = find(x, tar, 1, n);
    if (fa == tar) return tar;
    else return queryf(x, fa);
}

int queryrk(int x, int tar, int l, int r) {
    if (l == r) return rk[x].val;
    int m = l + r >> 1;
    if (tar <= m) return queryrk(lc(rk, x), tar, l, m);
    else return queryrk(rc(rk, x), tar, m + 1, r);
}

//tar更改目标元素,chg目标元素更改成的值
void updatef(int& x, int y, int tar, int chg, int l, int r) {
    x = ++cntf;
    f[x] = f[y];
    if (l == r) {
        f[x].val = chg;
        return;
    }
    int m = l + r >> 1;
    if (tar <= m) updatef(lc(f, x), lc(f, y), tar, chg, l, m);
    else updatef(rc(f, x), rc(f, y), tar, chg, m + 1, r);
}

//tar更改目标元素,add_val目标元素所加值
void updaterk(int& x, int y, int tar, int add_val, int l, int r) {
    x = ++cntrk;
    rk[x] = rk[y];
    if (l == r) {
        rk[x].val += add_val;
        return;
    }
    int m = l + r >> 1;
    if (tar <= m) updaterk(lc(rk, x), lc(rk, y), tar, add_val, l, m);
    else updaterk(rc(rk, x), rc(rk, y), tar, add_val, m + 1, r);
}

void combine(int nowver, int a, int b) {
    int fa1 = queryf(rootf[nowver - 1], a);
    int fa2 = queryf(rootf[nowver - 1], b);
    if (fa1 != fa2) {
        int rk1 = queryrk(rootrk[nowver - 1], fa1, 1, n);
        int rk2 = queryrk(rootrk[nowver - 1], fa2, 1, n);
        if (rk1 < rk2) swap(fa1, fa2), swap(rk1, rk2);
        updatef(rootf[nowver], rootf[nowver - 1], fa2, fa1, 1, n);
        updaterk(rootrk[nowver], rootrk[nowver - 1], fa1, rk2, 1, n);
    }
    else {
        rootf[nowver] = rootf[nowver - 1];
        rootrk[nowver] = rootrk[nowver - 1];
    }
}

void solve() {
    cin >> n >> m;
    buildf(rootf[0], 1, n);
    buildrk(rootrk[0], 1, n);
    for (int i = 1;i <= m;++i) {
        int op;cin >> op;
        if (op == 1) {
            int a, b;cin >> a >> b;
            combine(i, a, b);
        }
        else if (op == 2) {
            int k;cin >> k;
            rootf[i] = rootf[k];
            rootrk[i] = rootrk[k];
        }
        else {
            int a, b;cin >> a >> b;
            rootf[i] = rootf[i - 1];
            rootrk[i] = rootrk[i - 1];
            if (queryf(rootf[i], a) == queryf(rootf[i], b))
                cout << 1 << endl;
            else
                cout << 0 << endl;
        }
    }
}
```
## 数论
#### 2. 连续区间乘法逆元
```c++
const int MOD = 1e9 + 7;
const int N = 1e5 + 1;
int n;
ll inv[N];

void init() {
    inv[1] = 1;
    for (int i = 2;i < N;++i)
        inv[i] = (MOD - MOD / i) * inv[MOD % i] % MOD;
}
```
### 四、素性测试
#### 2. 3次检测的费马素性测试
```c++
ll bipow(ll base, ll p, ll mod) {
    ll ans = 1;
    while (p > 0) {
        if (p & 1) ans = ans * base % mod;
        base = base * base % mod;
        p >>= 1;
    }
    return ans;
}

//完全判断对于Carmichael数也无法判断

//3次检测的费马素性测试
//利用费马小定理
bool fermat(int n) {
    int flag = 0;
    for (int i = 1;i <= 3;++i) {
        srand(time(nullptr));
        ll a = rand() % (n - 2) + 2;
        if (bipow(a, n - 1, n) != 1)
            return false;
    }
    return true;
}

void solve() {
    ll n;cin >> n;
    if (fermat(n))
        cout << "YES" << endl;
    else
        cout << "NO" << endl;
}
```
#### 3. 米勒拉宾素性测试
```c++
ull bipow(ull base, ull p, ull mod) {
    ll ans = 1;
    while (p > 0) {
        if (p & 1) ans = ans * base % mod;
        base = base * base % mod;
        p >>= 1;
    }
    return ans;
}

//三次检验的米勒拉宾素性测试
bool Miller_Rabin(ull n,int times) {
    ull val = n - 1;
    ull p = 0;
    ull flag = 0;
    while ((val & 1) == 0) {
        val >>= 1;
        p++;
    }
    ull mp[7] = { 2,3,5,7,11,311,331 };
    for (int i = 0;i < times;++i) {
        if (mp[i] == n) return true;
        ull ret = bipow(mp[i], val, n);
        if (ret == 1) {
            flag++;
            continue;
        }
        for (ull k = 0;k < p;++k, ret = ret * ret % n)
            if (ret == n - 1) {
                flag++;
                break;
            }
    }

    if (flag == times) return true;
    return false;
}

inline void solve() {
    int n;cin >> n;
    for (int i = 1;i <= n;++i) {
        ull val;cin >> val;
        if (val == 1) {
            cout << "No" << endl;
            continue;
        }
        if (Miller_Rabin(val, 3))
            cout << "Yes" << endl;
        else
            cout << "No" << endl;
    }
}
```
### 六、组合数
#### 1. 快速幂 乘法逆元
```c++
//预处理 o(Nlog(MOD))
//查询   o(1)

const ll MOD = 1e9 + 7;
const ll N = 1e4 + 1;
ll q;
ll f[N], g[N];

//快速幂
ll qpow(ll base, ll p) {
    ll ans = 1;
    while (p > 0) {
        if (p & 1) ans = ans * base % MOD;
        base = base * base % MOD;
        p >>= 1;
    }
    return ans;
}

//乘法逆元
ll inv(ll x) {
    return qpow(x, MOD - 2);
}

//预处理
void init() {
    f[0] = g[0] = 1;
    for (ll i = 1;i < N;++i)
        f[i] = f[i - 1] * i % MOD;
    for (ll i = 1;i < N;++i) {
        g[i] = inv(f[i]);
    }
}

//求组合数
ll C(ll n, ll m) {
    return f[n] * g[n - m] % MOD * g[m] % MOD;
}

void solve() {
    init();

    cin >> q;
    for (ll i = 1;i <= q;++i) {
        ll n, m;
        cin >> n >> m;
        cout << C(n, m) << endl;
    }
}
```
#### 2. 连续区间乘法逆元
```c++
//预处理 o(N)
//查询   o(n)

const ll MOD = 1e9 + 7;
const ll N = 1e4 + 1;
ll q;
ll inv[N];

void init() {
    inv[1] = 1;
    for (ll i = 2;i < N;++i)
        inv[i] = (MOD - MOD / i) * inv[MOD % i] % MOD;
}

ll C(ll n, ll m) {
    ll ans = 1;
    for (ll i = n;i > n - m;--i)
        ans = ans * i % MOD;
    for (ll i = 1;i <= m;++i)
        ans = ans * inv[i] % MOD;
    return ans;
}

void solve() {
    init();

    cin >> q;
    for (ll i = 1;i <= q;++i) {
        ll n, m;
        cin >> n >> m;
        cout << C(n, m) << endl;
    }
}
```
#### 3. 杨辉三角 记忆化递推
```c++
//预处理 o(n^2)
//查询   o(1)

const ll MOD = 1e9 + 7;
const ll N = 1e4 + 1;
ll c[N][N];
ll q;

ll C(ll n, ll m) {
    if (n == m || n == 0) return 1;
    if (m > n >> 1) m = n - m;
    if (c[n][m]) return c[n][m];
    return c[n][m] = (C(n - 1, m) + C(n - 1, m - 1)) % MOD;
}

void solve() {
    cin >> q;
    for (int i = 1;i <= q;++i) {
        int n, m;
        cin >> n >> m;
        cout << C(n, m) << endl;
    }
}
```
#### 4. lucas
```c++
//预处理 o(MOD)
//查询   o(MOD*log(MOD)+log MOD(n))

const ll MOD = 10007;
const ll N = 1e4 + 1;
ll q;
ll f[MOD], g[MOD];

ll qpow(ll base, ll p) {
    ll ans = 1;
    while (p > 0) {
        if (p & 1) ans = ans * base % MOD;
        base = base * base % MOD;
        p >>= 1;
    }
    return ans;
}

ll inv(ll x) {
    return qpow(x, MOD - 2);
}

void init() {
    f[0] = g[0] = 1;
    for (int i = 1;i < MOD;++i) {
        f[i] = f[i - 1] * i % MOD;
        g[i] = g[i - 1] * inv(i) % MOD;
    }
}

ll getC(ll n, ll m) {
    if (m > n) return 0;
    return f[n] * g[n - m] % MOD * g[m] % MOD;
}

ll C(ll n, ll m) {
    if (m == 0) return 1;
    return C(n / MOD, m / MOD) * getC(n % MOD, m % MOD) % MOD;
}

void solve() {
    init();

    cin >> q;
    for (ll i = 1;i <= q;++i) {
        ll n, m;
        cin >> n >> m;
        cout << C(n, m) << endl;
    }
}
```
### 九、FFT
#### 1. 递归版本
```c++
//递归实现FFT和IFFT
void fft(complex<ld> A[], int n, bool inverse) {
    if (n == 1) return;
    complex<ld> A1[n / 2], A2[n / 2];
    for (int i = 0;i < n / 2;++i) {
        A1[i] = A[2 * i];
        A2[i] = A[2 * i + 1];
    }
    fft(A1, n / 2, inverse);
    fft(A2, n / 2, inverse);
    int flag = inverse ? -1 : 1;
    complex<ld> w1{ cos(2 * PI / n),sin(2 * PI / n) * flag };
    complex<ld> wk{ 1,0 };
    for (int i = 0;i < n / 2;++i) {
        A[i] = A1[i] + A2[i] * wk;
        A[i + n / 2] = A1[i] - A2[i] * wk;
        wk *= w1;
    }
}
```
#### 2. 非递归版本
```c++
const ld PI = 3.141592653589793;
const int N = 1e5 + 1;
int R[N];
int m;

void change(complex<ld> A[], int n) {
    for (int i = 0;i < n;++i)//位逆序变换
        R[i] = R[i / 2] / 2 + ((i & 1) ? n / 2 : 0);
    for (int i = 0;i < n;++i)
        swap(A[i], A[R[i]]);
}

//非递归实现FFT和IFFT
void fft(complex<ld> A[], int n, bool inverse) {
    change(A, n);
    int flag = inverse ? -1 : 1;
    for (m = 2;m <= n;m <<= 1) {
        complex<ld> w1{ cos(2 * PI / m),sin(2 * PI) / m * flag };
        for (int i = 0;i < n;i += m) {
            complex<ld> wk{ 1,0 };
            for (int j = 0;j < m / 2;++j) {
                complex<ld> x = A[i + j];
                complex<ld> y = A[i + j + m / 2] * wk;
                A[i + j] = x + y;
                A[i + j + m / 2] = x - y;
                wk *= w1;
            }
        }
    }
}
```
#### FFT 使用示例
```c++
void solve() {
    complex<ld> a[8]{ {1,0},{2,0},{3,0},{4,0} };
    complex<ld> b[8]{ {4,0},{3,0},{2,0},{1,0} };
    fft(a, 8, 0);
    fft(b, 8, 0);
    for (int i = 0;i < 8;++i) a[i] *= b[i];
    fft(a, 8, 1);
    for (int i = 0;i < 8;++i) {
        cout << a[i].real() / 8 << ' ' << a[i].imag() << endl;
    }
}
```
### 十、数论分块（整除分块）
```c++
const ll N = 1e9 + 1;
ll n, k;
ll sum;

void solve() {
    cin >> n >> k;
    sum = n * k;
    for (ll l = 1, r;l <= n;l = r + 1) {
        if (k / l == 0) break;
        r = min(k / (k / l), n);
        ll val = k / l;
        sum -= (r - l + 1) * (l + r) * val / 2;
    }

    cout << sum << endl;
}
```
## 搜索
### 一、bfs
#### 1. 单调队列优化记忆化广搜
```c++
const int N = 101;
int m, n;
int c[N][N];
bool vis[N][N];

typedef struct {
    int x, y;
    int coin;
    int color;
    bool magic;
    bool rev;
}Node;

int ans = INF;

class cmp {
public:
    inline bool operator()(Node& x, Node& y) {
        return x.coin > y.coin;
    }
};

void bfs() {
    priority_queue<Node, vector<Node>, cmp> q;
    q.push({ 1,1,0,c[1][1],0 });
    vis[1][1] = true;
    while (!q.empty()) {
        Node p = q.top();
        q.pop();
        if (vis[p.x][p.y] && p.magic == 1 || (vis[p.x][p.y] && p.rev)) continue;
        vis[p.x][p.y] = true;

        if (p.x == m && p.y == m) {
            ans = p.coin;
            return;
        }

        //向下
        if (p.y < m) {
            if (c[p.x][p.y + 1] == -1 && p.magic == 0)
                q.push({ p.x,p.y + 1,p.coin + 2,p.color,1,0 });
            if (c[p.x][p.y + 1] == p.color)
                q.push({ p.x,p.y + 1,p.coin,p.color,0,0 });
            else if (c[p.x][p.y + 1] != -1)
                q.push({ p.x,p.y + 1,p.coin + 1,c[p.x][p.y + 1],0,0 });
        }

        if (p.y > 1) {
            if (c[p.x][p.y - 1] == -1 && p.magic == 0)
                q.push({ p.x,p.y - 1,p.coin + 2,p.color,1,1 });
            if (c[p.x][p.y - 1] == p.color)
                q.push({ p.x,p.y - 1,p.coin,p.color,0,1 });
            else if (c[p.x][p.y - 1] != -1)
                q.push({ p.x,p.y - 1,p.coin + 1,c[p.x][p.y - 1],0,1 });
        }

        //向右
        if (p.x < m) {
            if (c[p.x + 1][p.y] == -1 && p.magic == 0)
                q.push({ p.x + 1,p.y,p.coin + 2 ,p.color,1,0 });
            if (c[p.x + 1][p.y] == p.color)
                q.push({ p.x + 1,p.y,p.coin,p.color,0,0 });
            else if (c[p.x + 1][p.y] != -1)
                q.push({ p.x + 1,p.y,p.coin + 1,c[p.x + 1][p.y],0,0 });
        }
        if (p.x > 1) {
            if (c[p.x - 1][p.y] == -1 && p.magic == 0)
                q.push({ p.x - 1,p.y,p.coin + 2 ,p.color,1,1 });
            if (c[p.x - 1][p.y] == p.color)
                q.push({ p.x - 1,p.y,p.coin,p.color,0,1 });
            else if (c[p.x - 1][p.y] != -1)
                q.push({ p.x - 1,p.y,p.coin + 1,c[p.x - 1][p.y],0,1 });
        }
    }
}

void solve() {
    cin >> m >> n;
    for (int i = 1;i <= m;++i)
        for (int j = 1;j <= m;++j)
            c[i][j] = -1;

    for (int i = 1;i <= n;++i) {
        int x, y, color;
        cin >> x >> y >> color;
        c[x][y] = color;
    }
    bfs();
    if (ans == INF)
        cout << -1 << endl;
    else
        cout << ans << endl;
}
```
### 二、meets in middle
#### 1. 多态
```c++
const int N = 21;
int n;
ll a[N];
int ldep, rdep;
map<ll, vector<ll>> mp;
bool s[1 << 21];
bool can[1 << 21];
ll ans;

void ldfs(int now, int sum,int idx) {
    if (now > ldep) {
        mp[sum].push_back(idx);
        return;
    }
    ldfs(now + 1, sum, idx);
    ldfs(now + 1, sum + a[now], idx | (1 << (now - 1)));
    ldfs(now + 1, sum - a[now], idx | (1 << (now - 1)));
}

void rdfs(int now, int sum,int idx) {
    if (now > rdep) {
        for (auto elem : mp[sum]) {
            ll index = (idx << ldep) | elem;
            can[index] = 1;
        }
        return;
    }
    rdfs(now + 1, sum, idx);
    rdfs(now + 1, sum + a[now + ldep], idx | (1 << (now - 1)));
    rdfs(now + 1, sum - a[now + ldep], idx | (1 << (now - 1)));
}

void solve() {
    cin >> n;
    for (int i = 1;i <= n;++i)
        cin >> a[i];
    ldep = n / 2;
    rdep = n - ldep;
    ldfs(1, 0, 0);
    rdfs(1, 0, 0);
    for (int i = 1;i < (1 << n);++i)
        if (can[i]) ans++;
    cout << ans << endl;
}
```
### 三、DLX (Dancing Links X)
#### 1. 模板
```c++
const int N = 5505;
int n, m, cnt;//cnt:1节点编号
int u[N], d[N], l[N], r[N];//四个方向的指针
int row[N], col[N];//节点所在的行数和列数
int h[N];//行表头
int s[N];//每列的节点数
int ans[N];//选取点

//初始化列表头
void init() {
    for (int y = 0;y <= m;++y) {
        u[y] = d[y] = y;
        l[y] = y - 1;
        r[y] = y + 1;
    }
    l[0] = m;
    r[m] = 0;
    cnt = m;
}

//二维双向链表插值
void link(int x, int y) {
    row[++cnt] = x;
    col[cnt] = y;
    s[y]++;
    //插入到双向链表的尾部 u[y]<->cnt<->y
    u[cnt] = u[y];
    d[u[y]] = cnt;
    d[cnt] = y;
    u[y] = cnt;
    if (h[x] == 0/*当前行无节点*/)
        h[x] = l[cnt] = r[cnt] = cnt;
    else {
        //插尾
        l[cnt] = l[h[x]];
        r[l[h[x]]] = cnt;
        r[cnt] = h[x];
        l[h[x]] = cnt;
    }
}

//删除列与关联行
void remove(int y) {
    r[l[y]] = r[y];
    l[r[y]] = l[y];
    //扫描方向没有强制要求
    for (int i = u[y];i != y;i = u[i])
        for (int j = l[i];j != i;j = l[j]) {
            //删除同列元素的一行，在up方向上
            d[u[j]] = d[j];
            u[d[j]] = u[j];
            s[col[j]]--;//在该列中删除该节点
        }
}

//恢复列与关联行
void resume(int y) {
    r[l[y]] = l[r[y]] = y;
    //扫描方向没有强制要求
    for (int i = u[y];i != y;i = u[i])
        for (int j = l[i];j != i;j = l[j]) {
            d[u[j]] = u[d[j]] = j;
            s[col[j]]++;
        }
}

bool dance(int dep) {
    if (r[0] == 0) {
        for (int i = 0;i < dep;++i)
            cout << ans[i] << ' ';
        cout << endl;
        return true;
    }

    //选出含节点最少的列
    int y = r[0];
    for (int i = r[0];i;i = r[i])
        if (s[i] < s[y]) y = i;

    remove(y);
    for (int i = d[y];i != y;i = d[i]) {
        ans[dep] = row[i];
        for (int j = r[i];j != i;j = r[j]) remove(col[j]);
        if (dance(dep + 1)) return true;
        for (int j = l[i];j != i;j = l[j]) resume(col[j]);
    }
    resume(y);
    return false;
}

void solve() {
    cin >> n >> m;
    init();
    
    for (int i = 1;i <= n;++i)
        for (int j = 1;j <= m;++j) {
            int t;cin >> t;
            if (t) link(i,j);
        }

    if (!dance(0))
        cout << "No Solution!" << endl;
}
```
## 字符串
### 一、字符串 hash
#### 1. 模板
```c++
const int MOD = 1e9 + 7;
const int P = 131;
const int N = 1e5 + 10;
int n, q;
char ch[N];
ll p[N];
vector<int> e[N];
ll val[N], inv_val[N];

int fa[N], son[N], top[N], sz[N], dep[N];

void dfs1(int u, int father) {
    fa[u] = father;
    sz[u] = 1;
    dep[u] = dep[father] + 1;
    val[u] = (val[father] * P % MOD + ch[u]) % MOD;
    inv_val[u] = (inv_val[father] % MOD + ch[u] * p[dep[u] - 1] % MOD) % MOD;
    // cout << u << ' ' << val[u] << ' ' << inv_val[u] << endl;
    for (auto v : e[u]) {
        if (v == father) continue;
        dfs1(v, u);
        sz[u] += sz[v];
        if (sz[son[u]] < sz[v]) son[u] = v;
    }
}

void dfs2(int u, int t) {
    top[u] = t;
    if (son[u]) dfs2(son[u], t);
    for (auto v : e[u]) {
        if (v == fa[u] || v == son[u]) continue;
        dfs2(v, v);
    }
}

int lca(int u, int v) {
    while (top[u] != top[v]) {
        if (dep[top[u]] < dep[top[v]]) swap(u, v);
        u = fa[top[u]];
    }
    return dep[u] < dep[v] ? u : v;
}

ll qpow(ll base, ll p, ll mod) {
    ll ans = 1;
    while (p) {
        if (p & 1) ans = ans * base % mod;
        base = base * base % mod;
        p >>= 1;
    }
    return ans;
}

ll inv(ll u, ll mod) {
    return qpow(u, mod - 2, mod);
}

ll gethash(int u, int v, int LCA) {
    ll p1 = ((val[u] - val[LCA] * p[dep[u] - dep[LCA]] % MOD) + MOD) % MOD;
    ll p2 = (inv_val[v] - inv_val[fa[LCA]] + MOD) % MOD;
    p2 = p2 * inv(p[dep[fa[LCA]]], MOD) % MOD;
    p2 = p2 * p[dep[u] - dep[LCA]] % MOD;
    return (p1 + p2 + MOD) % MOD;
}

void solve() {
    cin >> n;
    p[0] = 1;
    for (int i = 1;i <= n;++i) {
        cin >> ch[i];
        p[i] = p[i - 1] * P % MOD;
    }
    for (int i = 1;i <= n;++i) {
        int f;cin >> f;
        e[f].push_back(i);
    }

    dfs1(1, 0);
    dfs2(1, 1);

    cin >> q;
    for (int i = 1;i <= q;++i) {
        int l, r;
        cin >> l >> r;
        int LCA = lca(l, r);
        ll t1 = gethash(l, r, LCA);
        ll t2 = gethash(r, l, LCA);
        //cout << t1 << ' ' << t2 << endl;
        if (t1 == t2)
            cout << "YES" << endl;
        else
            cout << "NO" << endl;
    }
}
```
### 三、k短路
#### A* 算法
```c++
const int N = 1e5 + 1;
const int M = 5e5 + 1;
int n, m, k;
int s, t;

typedef struct {
    int to;
    int w;
    int next;
}Edge;
Edge e[M];
int h[N], rh[N];
int cnt;

void add(int* head, int u, int v, int w) {
    e[++cnt].to = v;
    e[cnt].w = w;
    e[cnt].next = head[u];
    head[u] = cnt;
}

class cmp {
public:
    bool operator()(PII& a, PII& b) {
        return a.second < b.second;
    }
};

int g[N];//估价函数
bool vis[N];
void dijkstra() {
    fill(g + 1, g + n + 1, INF);
    priority_queue<PII, vector<PII>, cmp> q;
    q.push({ t,0 });
    g[t] = 0;
    while (!q.empty()) {
        auto [u, dis] = q.top();
        q.pop();
        if (vis[u]) continue;
        g[u] = dis;
        vis[u] = true;
        for (int ed = rh[u];ed;ed = e[ed].next) {
            auto [v, w, next] = e[ed];
            if (!vis[v])
                q.push({ v,dis + w });
        }
    }
}

int f[N];//距离函数
int aStar() {
    priority_queue<PII, vector<PII>, cmp> q;
    q.push({ s,0 + g[s] });
    f[s] = 0;
    int tot = 0;

    while (tot < k || q.empty()) {
        auto [u, cost] = q.top();
        q.pop();
        f[u] = cost - g[u];
        if (u == t) tot++;
        for (int ed = h[u];ed;ed = e[ed].next) {
            auto [v, w, next] = e[ed];
            q.push({ v,w + g[v] + f[u] });//按照距离函数和估价函数之和排序
        }
    }
    if (tot == k)
        return f[t];
}

void solve() {
    cin >> n >> m;
    for (int i = 1;i <= m;++i) {
        int u, v, w;
        cin >> u >> v >> w;
        add(h, u, v, w);
        add(rh, v, u, w);
    }
    cin >> s >> t >> k;
    if (s == t) k++;
    dijkstra();
    if (g[s] == INF) cout << -1 << endl;
    else cout << aStar() << endl;
}
```
## 动态规划
### 二、区间dp
#### 例：石子合并
```c++
const int N = 301;
int mn[N][N], a[N];
int n;

void solve() {
    cin >> n;
    for (int i = 1;i <= n;++i)
        for (int j = 1;j <= n; ++j)
            mn[i][j] = INF;

    for (int i = 1;i <= n;++i) {
        int t;cin >> t;
        a[i] = a[i - 1] + t;
        mn[i][i] = 0;
    }

    for (int len = 2;len <= n;++len) {
        for (int l = 1;l + len - 1 <= n;++l) {
            int r = l + len - 1;
            for (int k = l;k < r;++k)
                mn[l][r] = min(mn[l][r], mn[l][k] + mn[k + 1][r] + a[r] - a[l - 1]);
        }
    }
    cout << mn[1][n] << endl;
}
```
#### 例：环形石子合并
```c++
const int N = 201;
int mn[N][N], mx[N][N], a[N];
int n;

void solve() {
    cin >> n;
    for (int i = 1;i <= 2 * n;++i)
        for (int j = 1;j <= 2 * n; ++j)
            mn[i][j] = INF;

    for (int i = 1;i <= n;++i) {
        int t;cin >> t;
        a[i] = a[i + n] = t;
    }
    for (int i = 1;i <= 2 * n;++i) {
        mn[i][i] = mx[i][i] = 0;
        a[i] += a[i - 1];
    }

    for (int len = 2;len <= n;++len)
        for (int l = 1;l + len - 1 <= 2 * n;++l) {
            int r = l + len - 1;
            for (int k = l;k < r;++k) {
                mn[l][r] = min(mn[l][r], mn[l][k] + mn[k + 1][r] + a[r] - a[l - 1]);
                mx[l][r] = max(mx[l][r], mx[l][k] + mx[k + 1][r] + a[r] - a[l - 1]);
            }
        }

    int ansmx = 0, ansmn = INF;
    for (int i = 1;i <= n;++i) {
        ansmx = max(ansmx, mx[i][i + n - 1]);
        ansmn = min(ansmn, mn[i][i + n - 1]);
    }

    cout << ansmn << endl;
    cout << ansmx << endl;
}
```
### 三、字符串dp
#### 1. 最长公共子串
```c++
const int N = 1e4 + 1;
char a[N], b[N];
int f[N];
int n, m;
int ans;

void solve() {
    cin >> n >> m;
    for (int i = 1;i <= n;++i)
        cin >> a[i];
    for (int i = 1;i <= m;++i)
        cin >> b[i];

    f[0] = 1;
    for (int i = 1;i <= n;++i)
        for (int j = m;j;--j) {
            if (a[i] == b[j])
                f[j] = max(f[j], f[j - 1] + 1);
            else
                f[j] = 0;
            ans = max(ans, f[j]);
        }

    cout << ans << endl;
}
```
#### 2. 最长公共子序列 LCS问题
* 滚动数组空间复杂度优化
```c++
const int N = 1e5 + 1;
int a[N], b[N];
int n;
int f[2][N];

void solve() {
    cin >> n;
    for (int i = 1;i <= n;++i)
        cin >> a[i];
    for (int i = 1;i <= n;++i)
        cin >> b[i];

    for (int i = 1;i <= n;++i) {
        for (int j = 1;j <= n;++j) {
            if (a[i] == b[j])
                f[i & 1][j] = max(f[i & 1][j], f[!(i & 1)][j - 1] + 1);
            else
                f[i & 1][j] = max(f[!(i & 1)][j], f[i & 1][j - 1]);
        }
    }
    cout << f[n & 1][n] << endl;
}
```
* 两个全排列数组的最长公共子序列问题
```c++
const int N = 1e5 + 1;
int a[N], b[N];
int mp[N];
int f[N];
int n, ans;

void solve() {
    cin >> n;
    for (int i = 1;i <= n;++i)
        cin >> a[i];
    for (int i = 1;i <= n;++i) {
        cin >> b[i];
        mp[b[i]] = i;
    }
    fill(f + 1, f + n + 1, INF);

    for (int i = 1;i <= n;++i) {
        auto ptr = lower_bound(f + 1, f + n + 1, mp[a[i]]);
        if (*ptr == INF) ans++;
        *ptr = mp[a[i]];
    }

    cout << ans << endl;
}
```
#### 3. 最长上升子序列 LIS问题
* 最长上升子序列 二维dp
```c++
//最长上升子序列 o(n^2)
const int N = 1e6 + 1;
int a[N], n;
int dp[N];
int ans;

void solve() {
    cin >> n;

    for (int i = 1;i <= n;++i) {
        cin >> a[i];
        dp[i] = 1;
    }
    for (int i = 2;i <= n;++i)
        for (int j = 1;j < i;++j)
            if (a[i] >= a[j]) {
                dp[i] = max(dp[i], dp[j] + 1);
                ans = max(ans, dp[i]);
            }

    cout << ans << endl;
}
```
* 最长上升子序列 二分法
```c++
const int N = 1e6 + 1;
int a[N], b[N];
int n, ans;

void solve() {
    cin >> n;
    for (int i = 1;i <= n;++i) {
        cin >> a[i];
        b[i] = INF;
    }

    for (int i = 1;i <= n;++i) {
        auto ptr = lower_bound(b + 1, b + n + 1, a[i]);
        if (*ptr == INF) ans++;
        *ptr = a[i];
    }
    cout << ans << endl;
}
```
#### 4. 编辑距离
```c++
const int N = 1e4 + 5;
char a[N], b[N];
int n, m;
int f[N][N];

void solve() {
    cin >> n >> m;
    for (int i = 1;i <= n;++i)
        cin >> a[i];
    for (int i = 1;i <= m;++i)
        cin >> b[i];

    for (int i = 1;i <= n;++i)
        f[i][0] = i;
    for (int i = 1;i <= m;++i)
        f[0][i] = i;

    for (int i = 1;i <= n;++i)
        for (int j = 1;j <= m;++j) {
            if (a[i] == b[j])
                f[i][j] = f[i - 1][j - 1];
            else
                f[i][j] = min(f[i - 1][j - 1], min(f[i - 1][j], f[i][j - 1])) + 1;
        }

    cout << f[n][m] << endl;
}
```
### 四、图上dp
#### 1. 例：完全食物链计数
```c++
const ll MOD = 80112002;
const ll N = 5e3 + 1;
ll n, m;
vector<ll> e[N];
ll indeg[N], odeg[N];
queue<ll> q;
ll dp[N];
vector<ll> ans;

void solve() {
    cin >> n >> m;
    for (ll i = 1;i <= m;++i) {
        ll u, v;
        cin >> u >> v;
        e[v].emplace_back(u);
        indeg[u]++;
        odeg[v]++;
    }
    for (ll i = 1;i <= n;++i) {
        if (!indeg[i]) {
            q.push(i);
            dp[i] = 1;
        }
        if (!odeg[i])
            ans.push_back(i);
    }

    while (!q.empty()) {
        ll u = q.front();
        q.pop();
        for (auto v : e[u]) {
            dp[v] = (dp[v] + dp[u]) % MOD;
            indeg[v]--;
            if (!indeg[v])
                q.push(v);
        }
    }
    ll ret = 0;
    for (auto u : ans)
        ret = (ret + dp[u]) % MOD;
    cout << ret << endl;
}
```